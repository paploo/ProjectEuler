#include "definitions.h"

/* Returns the integer square-root of a. */
UInteger intmath_sqrt(UInteger a);

/* Returns the absolute value of the integer. */
Integer intmath_abs(Integer a);

/* Returns the floor of the log2 of the unsigned integer given. */
Integer intmath_log2_floor(UInteger a);

/* Returns the ceiling of the log2 of the unsigned integer given. */
Integer intmath_log2_ceil(UInteger a);
